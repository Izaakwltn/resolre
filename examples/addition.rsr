;;;; smple addition example

mimimimi mi               ; incr c0 x5
do mi mi mi               ; incr c1 x3
                          ; (c1 is loop counter)
si
  re mi                   ; incr c0
  do fa                   ; decr c1 (counter)
ut                        ; loop end

                          ; adding 48 for ascii
mimimimi mimimimi         ; incr c1 x8
si
  re mimimimi mimi        ; incr c0 x 6
  do fa                   ; 1- counter
ut
re so                     ; print c0 => "8"
